package Practise_Testngg;

/*import org.apache.log4j.BasicConfigurator;
import org.apache.log4j.FileAppender;
import org.apache.log4j.HTMLLayout;

//import java.util.logging.Logger;

import org.apache.log4j.LogManager;
import org.apache.log4j.Logger;*/
//import org.apache.log4j.SimpleLayout;

public class Log4j_append{
	

	/*public static void main(String[] args) {
		
		BasicConfigurator.configure();
		Logger logs = LogManager.getLogger(Log4j_append.class.getName());
		FileAppender appender = new FileAppender();
		appender.setFile("logFile1.html");
		appender.setLayout(new HTMLLayout());//(new SimpleLayout());
		logs.addAppender(appender);
		appender.activateOptions();
		int x=0;
		while(x<5)
		{
			logs.debug("this is sample printing of logs" + x);
			x++;
		
		}
		
		

	}*/

}
